apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: mariadb-operator
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'mariadb-operator-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/its-software-services-customers/yru-moodle-addons.git
        targetRevision: "{{name}}" #Based on environment
        path: '03-argocd-apps/manifests/mariadb-operator'
        helm:
          skipCrds: true
          ignoreMissingValueFiles: true
          valueFiles:
            - values.yaml
            - values-{{name}}.yaml
      destination:
        name: in-cluster
        namespace: mariadb-operator
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
