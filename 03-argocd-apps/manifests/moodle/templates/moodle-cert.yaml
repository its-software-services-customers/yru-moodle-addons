apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: moodle-cert
spec:
  secretName: moodle-cert
  dnsNames:
    - {{ .Values.domain }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
